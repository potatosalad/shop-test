<%= form_for(@sucker) do |f| %>
  <% if @sucker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sucker.errors.count, "error") %> prohibited this sucker from being saved:</h2>

      <ul>
      <% @sucker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
