<%= form_for(@beak) do |f| %>
  <% if @beak.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beak.errors.count, "error") %> prohibited this beak from being saved:</h2>

      <ul>
      <% @beak.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
